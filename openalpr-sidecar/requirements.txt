fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
opencv-python-headless==4.9.0.80
numpy==1.26.4
# open-image-models[onnx] pulls in onnxruntime-cpu automatically.
open-image-models[onnx]==0.5.1
# fast-plate-ocr[onnx] pulls in onnxruntime-cpu automatically.
fast-plate-ocr[onnx]==1.1.0
# Optional easyocr fallback for graceful degradation when ONNX models
# fail to load. Default OFF via ENABLE_EASYOCR_FALLBACK.
easyocr==1.7.1
# PaddleOCR (PPOCRv4 recognizer) — strong on low-contrast / IR-illuminated
# plate text where fast-plate-ocr returns empty. Runs in parallel with the
# fast-plate-ocr stage; multi-model agreement boosts confidence. CPU-only
# build (~10x larger than fast-plate-ocr's ONNX but well within Railway
# memory limits).
paddleocr==2.8.1
paddlepaddle==2.6.2
# huggingface_hub for model downloads at build time.
huggingface_hub==0.24.7
# DETR-based plate detector. Heavy deps but the architecture handles
# small/angled plates better than YOLO. torch is installed separately
# in the Dockerfile via the CPU-only PyTorch index (cuts wheel size
# ~6x vs the default CUDA-bundled torch).
transformers==4.44.2
timm==1.0.9
